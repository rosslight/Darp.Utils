{
  "name": "mirrorsharp.demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild app.js app.css --bundle --outdir=../publish --minify && cp index.html ../publish"
  },
  "dependencies": {
    "@codemirror/search": "^6.5.11",
    "mirrorsharp-codemirror-6-preview": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
